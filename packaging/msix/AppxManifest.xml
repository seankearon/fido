<?xml version="1.0" encoding="utf-8"?>
<!--
  MSIX package manifest for Fido (full-trust Win32 desktop app).

  This is a TEMPLATE: the release workflow (.github/workflows/release.yml) substitutes
  the {VERSION} and {PUBLISHER} placeholders before handing the layout to makeappx.

    {VERSION}   - 4-part MSIX version "Major.Minor.Build.Revision" derived from the
                  SemVer release input (e.g. 1.2.0 -> 1.2.0.0).
    {PUBLISHER} - the X.500 subject of the signing certificate. It MUST match the cert
                  subject exactly or signing fails (default self-signed subject: CN=Fido).

  Logo images (Images\*.png) are copied from assets/png by the workflow, so the icon
  set has a single source of truth in the repo.
-->
<Package
    xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
    xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
    xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
    IgnorableNamespaces="uap rescap">

  <Identity
      Name="Fido"
      Publisher="{PUBLISHER}"
      Version="{VERSION}"
      ProcessorArchitecture="x64" />

  <Properties>
    <DisplayName>Fido</DisplayName>
    <PublisherDisplayName>Fido</PublisherDisplayName>
    <Logo>Images\StoreLogo.png</Logo>
    <Description>Resolves a branch name to its worktree on disk and opens it in Rider.</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.22621.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application Id="Fido" Executable="Fido.exe" EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
          DisplayName="Fido"
          Description="Resolves a branch name to its worktree on disk and opens it in Rider."
          BackgroundColor="transparent"
          Square150x150Logo="Images\Square150x150Logo.png"
          Square44x44Logo="Images\Square44x44Logo.png" />
    </Application>
  </Applications>

  <Capabilities>
    <!-- Win32 / full-trust desktop app packaged as MSIX. -->
    <rescap:Capability Name="runFullTrust" />
  </Capabilities>
</Package>
